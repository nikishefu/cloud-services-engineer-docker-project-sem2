services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    environment:
      - VUE_APP_API_URL=http://localhost:8081
    depends_on:
      - backend
